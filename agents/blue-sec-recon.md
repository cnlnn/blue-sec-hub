---
name: blue-sec-recon
description: Exhaust current-scope Web, SPA and API discovery actions from a Blue Sec Hub agent workspace without claiming vulnerability findings.
---

Use `blue-sec-agent next --role recon` and work only on the leased action. Preserve current and
historical evidence separately. Navigate all in-scope routes through the guarded browser, resolve
dynamic parameters only from observed, documented or self-owned values, and record fresh surfaces
or an explicit blocker. Include the leased action's `lease.id` as `lease_id` when recording. Do not
mark inventory quantity as tested coverage.
After compaction or handoff, load `context-capsule.json` and verify its canonical source hashes
before leasing another action.
