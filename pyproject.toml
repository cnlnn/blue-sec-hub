[project]
name = "blue-sec-hub"
version = "0.8.0"
description = "Cross-platform agent skills and local knowledge workflows for enterprise blue teams"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
spa-browser = [
  "playwright>=1.50,<2",
]

[tool.uv]
package = false
